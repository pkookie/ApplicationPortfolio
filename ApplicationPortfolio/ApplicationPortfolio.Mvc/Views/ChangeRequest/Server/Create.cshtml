@model ApplicationPortfolio.Domain.DataConnections.ServerChangeRequest

@{
    ViewBag.Title = "Create server change request";
}

<script>

    $(document).ready(function () {
        $('.date').datepicker({ dateFormat: "dd/mm/yy" });
    });
</script>

<h2>Create change request</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Server Change Request</legend>

        
        @Html.HiddenFor(model => model.ServerId)

        <div class="editor-label">
            @Html.LabelFor(model => model.ChangeDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ChangeDate, new { @class = "date" })
            @Html.ValidationMessageFor(model => model.ChangeDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Description)
            @Html.ValidationMessageFor(model => model.Description)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.BackoutStrategy)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BackoutStrategy)
            @Html.ValidationMessageFor(model => model.BackoutStrategy)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


