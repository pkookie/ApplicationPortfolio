@model IEnumerable<ApplicationPortfolio.Domain.DataConnections.Server>

@{
    ViewBag.Title = "Index";
}

<h2>Servers</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="GridDiv">
<table id="GridTable" class="data tableOrgoniser10-25-50-100-all" cellspacing="0">
       <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>
</thead>
        <tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServerId }) |
            @Html.ActionLink("Dependencies", "Details","Dependencies", new { id=item.ServerId,AppOrServer="Server" },null) |
            @Html.ActionLink("Change Requests", "Details", new { id=item.ServerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServerId })
        </td>
    </tr>
}
</tbody>
</table>

    </div>